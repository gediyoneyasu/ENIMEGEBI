import { AxiosInstance } from 'axios';
export declare function createAxiosInstance(secretKey: string, logging?: boolean, debug?: boolean, retries?: number, retryDelay?: number, timeout?: number): AxiosInstance;
