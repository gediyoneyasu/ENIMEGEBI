import { AxiosError } from 'axios';
export interface ChapaErrorResponse {
    message: string;
    status?: string;
    data?: unknown;
}
export declare function isAxiosError(error: unknown): error is AxiosError<ChapaErrorResponse>;
